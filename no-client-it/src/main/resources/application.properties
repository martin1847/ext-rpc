# A client app IS configured, and its scan MATCHES tech.krpc.ext.it.noclient.remote.RemoteService.
# See RemoteService's javadoc: this is deliberate, and it is what makes the guard strict — the
# client BuildSteps have everything they need EXCEPT rpc-client on the classpath.
quarkus.rpc.client.guardapp.url=http://127.0.0.1:59991
quarkus.rpc.client.guardapp.scan=tech.krpc.ext.it.noclient.remote
